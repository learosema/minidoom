# MINIDOOM

This is a basic 2.5D-engine in JavaScript using procedural noise textures in a single HTML file.

The name is a little bit misleading. It is not a complete implementation of the DOOM engine but a little proof-of-concept raycast engine.